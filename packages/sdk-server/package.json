{
  "name": "@xcloud/sdk-server",
  "version": "1.0.0",
  "description": "Smithy-generated server contracts (CJS) — consumed by backend services",
  "main": "./src/index.ts",
  "scripts": {
    "generate": "bash scripts/generate.sh"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
